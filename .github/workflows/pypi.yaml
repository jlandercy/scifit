name: Python package

on:
  push:
    branches:
      - main

jobs:

  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.10", "3.11"]

    steps:

      - name: Checkout
        uses: actions/checkout@v3

      - name: Create cache folder
        run: |
          mkdir -p .cache/reports
          mkdir -p .cache/media
          mkdir -p .cache/docs

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install nox build twine pip-tools
          pip-compile --extra dev pyproject.toml 
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Unit tests suite (classic)
        run: |
          python -m unittest -v

      - name: Unit tests suite (nox)
        run: |
          nox --session tests

#
#      - name: Documentation (nox)
#        run: |
#          nox --session notebooks docs
#Run nox --session notebooks docs
#  nox --session notebooks docs
#  shell: /usr/bin/bash -e {0}
#  env:
#    pythonLocation: /opt/hostedtoolcache/Python/3.11.4/x64
#    PKG_CONFIG_PATH: /opt/hostedtoolcache/Python/3.11.4/x64/lib/pkgconfig
#    Python_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.4/x64
#    Python2_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.4/x64
#    Python3_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.4/x64
#    LD_LIBRARY_PATH: /opt/hostedtoolcache/Python/3.11.4/x64/lib
#nox > Running session notebooks
#nox > python -m ipykernel install --name=venv
#nox > Command python -m ipykernel install --name=venv failed with exit code 1
#nox > Session notebooks failed.
#nox > Running session docs
#nox > sphinx-build -b html docs/source .cache/docs
#nox > Command sphinx-build -b html docs/source .cache/docs failed with exit code 2
#nox > Session docs failed.
#nox > Ran multiple sessions:
#nox > * notebooks: failed
#nox > * docs: failed
#Error: Process completed with exit code 1.

      - name: Create package
        run: |
          python -m build

      - name: Check package
        run: |
          twine check dist/*
